import React from 'react';
import { shallow } from 'enzyme';
import { IssueList } from '../IssueList'
import { Issue } from '../Issue';
import { Loader } from '../Loader';

describe('<IssueList />', () => {
  it('should render expected element for null issues', () => {

  })

  it('should render expected element for 0 issues', () => {

  })

  it('should render expected element for 1 issues', () => {

  })

  it('should render expected element for 5 issues', () => {

  })

  it('should render expected element when receiving error text', () => {
    
  })

});